name: flutter_form_validators
description: Sets of common validators and a way to make your own.
version: 0.0.1
repository: https://github.com/saurav64bit/flutter_form_validators
issue_tracker: https://github.com/saurav64bit/flutter_form_validators/issues
homepage: https://github.com/saurav64bit/flutter_form_validators

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  generate: true